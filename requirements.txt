# Minimal runtime dependencies for this application
# - SQLAlchemy: ORM and engine
# - python-dotenv: optional .env loading in session setup
# - rich: nicer CLI rendering (tables, panels). Optional fallback to plain text is implemented.
SQLAlchemy>=1.4,<3.0
python-dotenv>=0.21.0
rich>=13.0.0